# Example environment variables for api-gateway
# Copy to .env and customize. Do NOT commit your real .env.

# Server
SERVER_PORT=8080

# JWT (set your own secret; do not use real secrets in VCS)
JWT_SECRET=change-me
JWT_HEADER=Authorization

# Downstream service URLs (used by the gateway to proxy requests)
AUTH_SERVICE_URL=http://auth-service:8081
USER_MANAGEMENT_SERVICE_URL=http://user-management-service:8082
FEED_MANAGEMENT_SERVICE_URL=http://feed-management-service:8090
MESSAGE_MANAGEMENT_SERVICE_URL=http://message-management-service:8091

# Optional Kafka/SR if the gateway needs them in the future
SPRING_KAFKA_BOOTSTRAP_SERVERS=broker:29092
SCHEMA_REGISTRY_URL=http://schema-registry:8081


