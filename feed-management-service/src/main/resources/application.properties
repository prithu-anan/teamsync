# Server Configuration
server.port=8090
spring.application.name=feed-management-service

# Database Configuration
spring.datasource.url=jdbc:postgresql://feed_management_postgres:5432/feed_management_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# Multipart Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.level.edu.teamsync.usermanagement=DEBUG
logging.level.org.springframework.security=DEBUG

# # Project Service URL for OpenFeign client
# project-service.url=http://project-management-service:8083


# # User Service URL for OpenFeign client
# user-service.url=http://user-management-service:8082

# # Kafka Properties
# spring.kafka.bootstrap-servers=broker:29092
# spring.kafka.template.default-topic=user-deleted
# spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
# spring.kafka.producer.value-serializer=io.confluent.kafka.serializers.KafkaAvroSerializer
# spring.kafka.producer.properties.schema.registry.url=http://schema-registry:8081


azure.storage.connection-string=${AZURE_CONNECTION_STRING}
azure.storage.container-name=${AZURE_CONTAINER_NAME}
azure.storage.account-name=${AZURE_ACCOUNT_NAME}
azure.storage.sas-token=${AZURE_SAS_TOKEN}


